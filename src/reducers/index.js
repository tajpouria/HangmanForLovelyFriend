import { combineReducers } from 'redux';

import compare from './compare';

export default combineReducers({
  // which letters are already goal
  compare,
  // is this two match and witch letter should appear
});
